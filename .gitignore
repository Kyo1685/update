__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.DS_Store

# Live-stats on-disk cache
.stats_cache.json
*.stats_cache.json.tmp

# Per-device calibration (run calibrate.py to regenerate)
layout.json

# Personal avatar crops (keep the folder + its README, ignore the images)
templates/*.png
templates/*.jpg
templates/*.jpeg
templates/*.webp
