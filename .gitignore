__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.DS_Store

# Live-stats on-disk cache
.stats_cache.json
*.stats_cache.json.tmp

# Per-device calibration (run calibrate.py to regenerate)
layout.json

# A ready-to-use square template set is committed under templates/.
# Re-running fetch_templates.py / import_icons.py just overwrites the tracked
# files, so we no longer ignore them.

# Learned per-screen templates: seeds are force-added; auto-learned ones stay local
templates_learned/*
templates_learned_enemy/*
!templates_learned/nana.png
!templates_learned/melissa.png
!templates_learned/lukas.png
!templates_learned_enemy/vexana.png
!templates_learned_enemy/johnson.png
!templates_learned_enemy/layla.png
